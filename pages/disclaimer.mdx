---
comments: false
---

import { La_Belle_Aurore } from "next/font/google";
import cn from "../utils/cn";

export const laBelleAurore = La_Belle_Aurore({
  subsets: ["latin"],
  weight: "400",
});

export const Disclaimer = () => {
  return (
    <div className={cn(laBelleAurore.className, "mt-6 text-2xl font-bold")}>
      .
    </div>
  );
};

export const Title = () => {
  return (
    <h1
      className={cn(
        "_mt-2 _text-4xl _font-bold _tracking-tight _text-slate-900 dark:_text-slate-100",
        laBelleAurore.className,
      )}
    >
      ❤️
    </h1>
  );
};

<Title />

<Disclaimer />
